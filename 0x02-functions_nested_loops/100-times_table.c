#include "main.h"

/**
* print_times_table - function that prints the n times table, 
* starting with 0.
*
* Return: Always 0
*/
int print_times_table(void)
{
	if (n > 15)
	{
		_putchar(' ');
	}
	else if (n < 0)
	{
		 _putchar(' ');
	}
	return (0);
}
