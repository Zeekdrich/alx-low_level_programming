#include "main.h"

/**
* print_times_table - function that prints the n times table, 
* starting with 0.
*
* Return: Always 0
*/
void print_times_table(int n);
{
	if (n > 15)
	{
		_putchar(' ');
	}	
       	if (n < 0)
	{
		 _putchar(' ');
	}
	return ();
}
